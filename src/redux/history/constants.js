import {actionType} from '../actionType';

export const GET_RECENT_HISTORY_ACTION = actionType(
  'history/GET_RECENT_HISTORY_ACTION',
);

export const UPDATE_HISTORY_ACTION = actionType(
  'history/UPDATE_HISTORY_ACTION',
);

export const REMOVE_HISTORY_ACTION = actionType(
  'history/REMOVE_HISTORY_ACTION',
);
